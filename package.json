{
  "name": "eduplatform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm run dev:main-api\" \"pnpm run dev:worker\"",
    "dev:main-api": "pnpm --filter main-api run start:dev",
    "dev:worker": "pnpm --filter image-worker run dev",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
